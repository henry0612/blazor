@* 未認証時に /login へリダイレクトするコンポーネント *@
@rendermode InteractiveServer
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/login", forceLoad: true);
    }
}
